import styled from 'styled-components'

import { View } from 'react-native'

const LanguageSwitcherWrapper = styled(View)`
    position: relative;
    width: 100%;
`

export default LanguageSwitcherWrapper
